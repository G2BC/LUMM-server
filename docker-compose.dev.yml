version: '3.9'

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        ENV: development
    volumes:
      - .:/app
    ports:
      - "4000:4000"
    environment:
      - ENV=development
